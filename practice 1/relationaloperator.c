#include <stdio.h>
int main(){

    int a=5,b=3;
    
    printf("is a greater than b %d\n",a>b);
    printf("is a less than b %d\n",a<b);
    printf("is a equal to b %d\n",a==b);
    printf("is a less than b %d\n",a!=b);

    return 0;
}